<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
              <title>Add-Listing</title>

                {{>linkrel}}

       
       </head>
    <body>
<!-- Preloader -->
<div id="preloader"></div>
        <!-- Static navbar -->
        {{>header headerOption}}
        <div class="page-bread mb70">
            <div class="container">
                <div class="row">
                    <div class="col-sm-6">
                        <h3>submit listing</h3>
                    </div>
                    <div class="col-sm-6">

                    </div>
                </div>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-md-12 mb30">
                    <div class="mb40">
                        <h2 class="left-title">Description</h2>
                        <form id="form1" method="POST" action="/listing" enctype="multipart/form-data">
                            <div class="form-group">
                                <input class="form-control" type="text" placeholder="Title" name="title">
                            </div>
                            <div class="form-group">
                                <label for="categories">Choose a Category:</label>
                                <select class='form-control' id="categories" name="categories">
                                  {{#each category}}
                                  <option value={{category_id}}>{{category_name}}</option>
                                  {{/each}}
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="location">Choose a Location:</label>
                                <select class='form-control' id="location" name="location">
                                  {{#each location}}
                                  <option value={{location_id}}>{{location_name}}</option>
                                  {{/each}}
                                </select>
                            </div>
                            <div class="form-group-icon mb15">
                                <i class="fa fa-dollar"></i>
                                <input type="number" min="0" class="form-control" placeholder="Price" name="price">
                            </div>
                            <div class="form-group">
                                <textarea class="form-control" placeholder="Listing Description" rows="8" name="description"></textarea><div class="textarea-resize"></div>
                            </div>
                            <div class="mb40">
                                <div class="field">
                                    
                                    </script>
                                    <p><label class="form-control" for="pictures" style="cursor: pointer;">Upload Image</label></p>
                                    <input type="file" id="files" name="pictures" multiple>
                                </div>
                                
                            </div><!--/-->
                            
                         </div><!--/-->
                            </div>
                             <div class="text-right mb40">
                                <button id="submitad" class="btn btn-lg btn-primary" onclick="submitForms()">Submit Listing</button>
                    </div>
                    </form>
                   
                </div>
            </div>
        </div>
    </div>
</div><!--/col-->
       <!--footer-->
        {{>footer}}
        <!-- jQuery-->

        
        {{!-- <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB8VMR9FooFZN64_qR8pu0jY0NJ8j_sicE&libraries=places&callback=initAutocomplete" --}}
        {{!-- async defer></script> --}}
        
     <script>
         {{!-- $(document).ready(function() {
                                    if (window.File && window.FileList && window.FileReader) {
                                        $("#files").on("change", function(e) {
                                        var files = e.target.files,
                                            filesLength = files.length;
                                        for (var i = 0; i < filesLength; i++) {
                                            var f = files[i]
                                            var fileReader = new FileReader();
                                            fileReader.onload = (function(e) {
                                            var file = e.target;
                                            $("<span class=\"pip\">" +
                                                "<img class=\"imageThumb\" src=\"" + e.target.result + "\" title=\"" + file.name + "\"/>" +
                                                "<br/><span class=\"remove\">Remove image</span>" +
                                                "</span>").insertAfter("#files");
                                            $(".remove").click(function(){
                                                $(this).parent(".pip").remove();
                                            });
                                            
                                            });
                                            fileReader.readAsDataURL(f);
                                        }
                                        });
                                    } else {
                                        alert("Your browser doesn't support to File API")
                                    }
                             });</script> --}}
    </body>
</html>